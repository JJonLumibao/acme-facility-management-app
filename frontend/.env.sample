REACT_APP_API_URL=http://localhost:3001
REACT_APP_API_ENDPOINTS='{}'

# Used only when VITE_API_ENDPOINTS is empty (no LocalStack/Terraform deploy yet) - points the
# frontend directly at backend/api/dev_server.py (FastAPI dev server) for fast local iteration.
VITE_DEV_API_URL=http://localhost:8000
